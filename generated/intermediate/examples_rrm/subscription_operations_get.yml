- hosts: localhost
  roles:
    - ../modules
  vars_files:
    - _vars.yml
  vars:
  tasks:

    - name: getOperations
      azure_rm_subscriptionsoperation_info: {}
    
      register: output
    - debug:
        var: output